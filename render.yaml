services:
  - type: web
    name: polaras
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: OPENAI_MODEL
        value: gpt-5.4-mini
      - key: PORT
        value: 10000
